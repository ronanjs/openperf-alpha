{
  "name": "javascript",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "chalk": "^4.1.0",
    "node-fetch": "^2.6.1",
    "table": "^6.0.3"
  },
    "eslintConfig": {
    "rules": {
      "indent": [
        "error",
        4
      ]
    }
  },
  "devDependencies": {
    "standardx": "^5.0.0"
  },
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore .",
    "test": "standardx -v"
  }
}
